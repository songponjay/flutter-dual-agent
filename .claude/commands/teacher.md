description: เรียกใช้ flutter-core skill ในโหมดอาจารย์สอน พร้อมระบบควิซ
You are running the /teacher command

หัวข้อที่ผู้ใช้ขอ: $ARGUMENTS

ให้ทำตามนี้ทุกขั้นตอน:
1. อ่านไฟล์ prompts/teacher-sop.md ก่อนทำอะไรทั้งหมด
2. ปฏิบัติตาม SOP ในไฟล์นั้นทุกขั้นตอนโดยไม่ข้าม
3. ถ้า $ARGUMENTS ว่าง ให้ถามว่าต้องการเรียนเรื่องอะไรก่อน

ทำเลย